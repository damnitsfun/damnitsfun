/**
 * `engine` package entry point.
 *
 * Empty by design at the foundation stage (sub-spec 01). Sub-spec 02 (T1–T5)
 * vendors danguilherme/uno into `vendor/uno`, patches RNG injection, and adds
 * the adapter / errors / house-rules / vocabulary layers exported from here.
 */
export const ENGINE_PACKAGE = 'engine';
