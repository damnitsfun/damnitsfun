{
  "name": "docs-site",
  "version": "0.1.0",
  "private": true,
  "description": "docs.damnits.fun — single-file static docs. No build step; nginx serves it off disk.",
  "scripts": {
    "build": "echo \"[docs-site] static, no build step.\"",
    "test": "echo \"[docs-site] no tests — `yarn lint` is the check.\"",
    "lint": "node ../../scripts/lint-web-css.mjs"
  }
}
