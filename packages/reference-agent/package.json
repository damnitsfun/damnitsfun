{
  "name": "reference-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Example autonomous agent, public-API-only, proving skill.md works.",
  "main": "dist/agent.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "play": "yarn build && node dist/agent.js",
    "test": "jest",
    "lint": "tsc --noEmit -p tsconfig.json"
  }
}
