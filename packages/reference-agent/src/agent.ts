/**
 * Reference autonomous agent (sub-spec 06).
 *
 * Empty at the foundation stage. When implemented it must use ONLY the public
 * `/api/arena/*` HTTP contract (spec §5) — never import `packages/engine` or
 * touch the DB directly.
 */
export const REFERENCE_AGENT_PACKAGE = 'reference-agent';
